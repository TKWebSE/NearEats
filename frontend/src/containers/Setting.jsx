import { Fragment } from "react"
import media from "styled-media-query";
import styled from "styled-components";
import { SETTING_TEXT } from "../constants";

export const Setting = () => {

  return (
    <Fragment>
      {SETTING_TEXT.USER_DELETE_LINK_TEXT}
    </Fragment>
  )
}
