import React, {Fragment,useEffect} from 'react'

export const OrderDetail = ({match}) => { 

  useEffect(() => {
    dispatchEvent({})
    
  },[])

  return (
    <Fragment>
        sampleCページ
    </Fragment>
  )
}